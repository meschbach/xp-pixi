# Tasks: Tower Target Visuals

## 1. Simulation platform (outbox + entity identity)

- [x] 1.1 Unify entity identity: replace `nextTowerId` / `nextEnemyId` in `world.ts` with a single `nextEntityId` counter; assign tower ids in `placement.ts` and enemy ids in `world.ts` from it; update tests that assume per-domain counter starts
- [x] 1.2 Add combat event types and the outbox to `World` (design D11): `CombatEvent` type with `targetCell`, `targetToCell`, and `targetProgress` fields under `src/simulation` (sim-safe only, no pixi/DOM imports); `World` interface gains `combatEvents` (readonly array of live, untrimmed, oldest-first events), `emitShot(event)` (appends with a **never-resetting monotonic id**, D1; **no value import into towers.ts**), and `ackEvents(upToId)` (monotonic ack cursor); internal head offset recycles storage at the top of each tick (D1/D2/D10)
- [x] 1.3 Publish the shot fact in `tickTowers` at the fire point via `world.emitShot(...)` (**no new import into towers.ts** — use the `world` parameter already in scope): `{kind:'shot', tick: world.tickCount, towerId: tower.id, targetId: target.id, targetCell: target.fromCell, targetToCell: target.toCell, targetProgress: target.progress}` (design D5); damage/cooldown/targeting behavior unchanged
- [x] 1.4 Add the acknowledgment surface: consumer advances a monotonic ack cursor; non-advancing acks are ignored; at the top of each tick the outbox reclaims every entry at or below the lowest acked id (design D2)
- [x] 1.5 Add/exempt sim tests: one event per attack with correct payload (incl. `targetToCell`); monotonic ids ordered by tick and **never reset or reuse across trims/HMR world survival**; ack/trim keeps the outbox bounded; a non-advancing ack is ignored; trimming never reclaims an event a live consumer hasn't drained; consumer starting at the head receives nothing pre-existing; entity ids unique across towers and enemies; existing cooldown/retarget/kill tests unchanged and green

## 2. Render-side pure helpers

- [x] 2.1 Extract aim-angle helper (tower center → target **interpolated drawn position** `from + (to − from) × progress`; handle the degenerate self-cell case where from/to are the same) into a pure module; unit-test including the self-cell edge and a mid-hop target
- [x] 2.2 Extract an outbox drain/watermark helper (seed at head on subscribe, drain forward, return new events + next cursor); unit-test start-at-head and empty-outbox cases
- [x] 2.3 Define per-tower identity booking (design D8/D13): each tower's accent color, tether dash pattern, and enemy marker glyph keyed together by `id % N` (one identity object so the three cues never drift out of phase); accent palette cyan `0x22d3ee`, magenta `0xcc55ff`, rose `0xee2d8a`, spring green `0xb8e986`, teal `0x0d9aa3` (verified via `npm run cvd-check`, D8); and a `typeId → { shape: 'triangle' | ... }` registry with default fallback for unknown types (D9); unit-test known/unknown type resolution, identity stability across id sequences, and that identical ids yield identical (color, pattern, glyph) bookings
- [x] 2.4 Extract pure structural-link geometry helpers (design D15/D16): (a) enemy-marker fan-out — given tower id, enemy center, target count `N`, return a stable regular-polygon pixel offset (N=1 centered, N=2 left/right, N=3 triangle, N=4 square); (b) tether pulse — given tower id and time-in-ticks, return a 0→1 sawtooth progress with an id-based phase offset. Unit-test fan-out stability across id sequences and counts, and pulse phase distinctness for adjacent ids

## 3. Scene presentation

- [x] 3.1 Orient towers: eased turn toward the current target; bounded-rate idle settle back to neutral "up"; orientation easing advances on the **sim clock** (`world.tickCount` delta), not wall-clock (design D12; spec "Aim orientation and idle settle")
- [x] 3.2 Draw persistent targeting: per-tower accent ring on the tower, faint tether and enemy marker at the target's **interpolated drawn position** (`from + (to − from) × progress`), placed at the **fan-out offset** for the target count (D15, task 2.4), using the tower's **dash pattern and marker glyph** from the identity book (D8/D13); draw the **dim phase-keyed pulse** traveling along each tether on the sim clock (D16, task 2.4); maintain the mapping-triangle topology — tether connects tower center to the offset marker point, persistent layers under the transient beam (D17); for the hit-ring (transient, task 3.3) reconstruct fire-time position from `targetCell` + `targetToCell` + `targetProgress` (D5/D7); nothing shown when a tower has no target; an unresolvable `targetId` (just-killed enemy) renders as no target rather than erroring
- [x] 3.3 Consume shot events each frame through the watermark helper to spawn **tick-keyed** muzzle flash + beam + impact ring (beam endpoints from `targetCell` + `targetToCell` + `targetProgress`; beam uses the firing tower's accent color as the bright layer, D7/D13); combat effects age against `world.tickCount` (closure-local `lastTickCount` seeded at creation, per-frame `tickDelta = world.tickCount − lastTickCount`, effects carry `ageTicks` and expire at `TTL_TICKS`); **ack must happen after all effects are spawned** in the frame (drain → spawn → draw → ack, so the sim never reclaims a not-yet-spawned event); remove the `lastHpById` HP-drop heuristic **and** its stale-id pruning block (the `seenIds` cleanup); delete the `addFlash` public API and `FlashEffect` struct (replaced by tick-keyed ring in `fxG`); `FLASH_TTL_MS` becomes a tick-count equivalent
- [x] 3.4 Render towers by shape registry (`typeId` → shape, triangle today); preserve existing coverage tint, hover/selection outlines, and z-order across the new layers

## 4. Integration and verification

- [x] 4.0 Create `docs/visual-language.md` (design D14): reserved color table (every current color constant and meaning), the redundant-coding principle (D8: identity = pattern + luminance + structure; color is enhancement), and the CVD-simulation gate (Machado 2009 deuteranopia/protanopia/tritanopia) recorded as the standard check for any future color; add the checked-in `scripts/cvd-check.mjs` tool wired as `npm run cvd-check` so the palette is re-verifiable, keeping its `RESERVED`/`ACCENTS` tables in lock-step with the doc and D8
- [x] 4.1 Confirm wiring: scene view receives the world's outbox each frame and seeds its watermark at the outbox head on creation (design D3); `main.ts` unchanged
- [x] 4.2 HMR sanity (manual): edit a rendering module mid-run; the carried world keeps its events, no pre-existing events replay as beams (editing a simulation module requires a full reload; the carried world runs its old closures until then — accepted, D/Risks)
- [x] 4.3 Visual pass (manual): a 3-tower focus-fire sliver reads as "one enemy, several colors + tether patterns"; idle settle and beam timing look right; tether patterns/marker glyphs decode the mapping even if hues are ambiguous, cross-checked via the CVD gate in `docs/visual-language.md`
- [x] 4.4 Quality gates: `npm run typecheck`, `npm run lint`, `npm test` green; simulation modules add no pixi/DOM imports (purity intact)
- [x] 4.5 End-to-end sanity (manual): build, fire, focus fire, idle, restart; outbox stays bounded through a full campaign (no growth over time)